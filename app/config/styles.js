import f from './fonts'

const styles = {
  containerTotal: {
    flex: 1,
  },
  textError: {
    color: 'red',
    fontSize: f.button
  },
  textTitle: {
    fontFamily: f.family,
    fontSize: f.appBar,
  },
  textSubtitle: {
    fontFamily: f.family,
    fontSize: f.subHeading,
  }
}

export default styles